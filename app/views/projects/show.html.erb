<h1>
  <%=h @project.name %>
</h1>

<p>
  <%=h(@project.description).gsub("\n","<br />") if @project.description %>
</p>

<h2>Usuários associados</h2>
<ul>
  <% @project.users.each do |user| %>
  <li><%= link_to user.name, user %></li>
  <% end %>
  <li><%= link_to_unless @project.users.include?(@current_user), 'Associar-se', 
           new_project_project_memebership_path(@project) %></li>
</ul>

<%= link_to 'Editar', edit_project_path(@project) %>
